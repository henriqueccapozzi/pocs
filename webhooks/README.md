Make a simple lab setup to ilustrate and test a webhook
implementation

Ideally implement both the server and the client, but it's
valid to start with just the client implementation that is 
more commonly used
